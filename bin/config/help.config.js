module.exports.config = {
    main: `
        Usage: yarn elmerBuilder [command] [flags]
        command: 
            init                        Initialize the project creation project structure and configuration files
            help                        Show the current plug-in of all supported commands
            version                     output the version number
        Options:
            -v,--version                output the version number
            -init                       Initialize the project creation project structure and configuration files            
    `
};
